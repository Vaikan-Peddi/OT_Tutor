# Environment & secrets
.env

# SQLite runtime DB — excluded (ephemeral on server)
db/*.db

# ChromaDB is committed so Render doesn't need the PDF

# Eval outputs
eval_results/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Data (large PDF)
data/*.pdf

# Jupyter leftovers
.ipynb_checkpoints/
*.ipynb


web/frontend/node_modules/
